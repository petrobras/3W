#!/bin/bash
set -ex

cd "$(dirname "$0")/.."

: "${RUN_SLOW_TESTS:=0}"
echo "RUN_SLOW_TESTS is set to $RUN_SLOW_TESTS"

pytest tests \
  --cov=ThreeWToolkit \
  --cov-report=term \
  --cov-report=xml:coverage.xml
